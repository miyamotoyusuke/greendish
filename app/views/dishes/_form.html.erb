<%= form_with(model: dish) do |f| %>
  <%= render 'layouts/error_messages', model: f.object %>

  <%= f.label :content, '料理名' %>
  <%= f.text_field :content %>
  <%= f.label :vegetables, '野菜の種数' %>
  <%= f.text_field :vegetables %>
  
  <%= f.submit '記録' %>
<% end %>